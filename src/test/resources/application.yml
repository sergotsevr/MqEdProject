spring:
  main:
    log-startup-info: false
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
kafka:
  group: myGroup
  topic: top
mq:
  type: "kafka"
rabbit:
  host: localhost
  queue: queue1
logging:
  level:
    org:
      springframework: INFO
    root: INFO